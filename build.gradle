// Fugo Client - Multi-Module Parent Project Build File

subprojects {
    repositories {
        mavenCentral()
        maven {
            name = "Modrinth"
            url = "https://api.modrinth.com/maven"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
